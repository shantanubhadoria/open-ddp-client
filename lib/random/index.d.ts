export declare class Random {
    static hexString(digits: number): string;
    static randomString(charsCount: number, alphabet: string): string;
    static choice(arrayOrString: Array<string> | string): string;
}
